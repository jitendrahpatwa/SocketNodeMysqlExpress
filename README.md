# Socket.io NodeJS Express Mysql

```
npm install socket.io
npm install mysql
npm install express
```
OR
```
npm install --save
```

## index.html contains the on event for socket with chat and returns mysql db record in a table.

